# AGENTS.md

gint 是严格固定位宽的 C++11 header-only 宽整数库。开始任务先阅读并遵守
[贡献指南](CONTRIBUTING.md)，再按受影响范围读取对应契约章节；文档职责见
[文档导航](docs/README.md)。

## 先确认契约

- 公共语义由[技术规格](docs/TECH_SPEC.md)定义，保持实际位宽、补码与回绕语义。
- 语言、平台和产品边界由[支持策略](docs/SUPPORT.md)定义，不自行扩大支持范围。
- 修改实现从 `src/gint/` 入手；`include/gint/gint.h` 是生成交付物。
  内部头及生成器输入约束见[实现说明](docs/INTERNALS.md)。
- 性能工作遵守[基准测试](docs/BENCHMARKS.md)，版本同步遵守
  [发布流程](docs/RELEASING.md)。实现、测试和权威文档必须一致。

## 授权与工作区

- 回答、解释、审阅、诊断或规划任务只检查材料并报告结果；除非用户同时要求修改，
  不实现修复、不改变仓库或远端状态。
- 修改、修复或构建任务可直接完成范围内的本地编辑，并运行相关非破坏性验证。
- 已授权范围内的常规实现选择自行决定并持续完成；只有缺失信息会实质改变范围、
  公共语义或不可逆结果时才询问。
- 外部写入、提交、推送、创建或合并 PR、发布、破坏性操作、购买，以及实质扩展
  任务范围，都需要用户明确授权；会话中已有授权在未被撤回或变更时持续有效，
  无需重复确认。
- 保留无关修改、现有 worktree 和产物；`third_party/` 默认只读，除非任务明确
  要求更新依赖。
- 构建、依赖、缓存、日志和临时文件放入 `runs/<scope>/`；不同 OS、架构或编译器
  不得复用 CMake cache。

## 完成与报告

- 先确定受影响契约，按[验证表](CONTRIBUTING.md#按风险选择验证)完成最小充分门禁。
- 必需检查通过后，仅因新修改、失败或未解决疑点扩大或重复验证。
- 请求的行为、测试和权威文档一致，没有夹带范围外修改。
- 报告修改文件、关键验证证据、未验证项和 worktree 状态；检查无法运行时说明
  原因、影响及下一项有效验证，不将单一环境结果外推到其他支持组合。
- PR 按贡献指南使用匹配模板与标题。
