<!--
PR 标题必须使用 `<类型>: <简要描述>`，类型限 `feat`、`fix`、`perf`、`refactor`、`docs`、`break`。
禁止添加 `[codex]`、`AI` 等额外前缀或后缀。
若本次变更清晰归属某一类，优先使用 `.github/PULL_REQUEST_TEMPLATE/` 下的专用模板。
通过 API、CLI 或 connector 创建 PR 时，请手动套用模板，并用 `gh pr view --json title,body` 核对。
-->

## 概述

-

## 做了什么

-

## 验证

-

## 影响与风险

-
